[build]
  base    = "sh.client"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — mọi route đều trả index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
